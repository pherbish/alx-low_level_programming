#include "main.h"
/**
 * *string_toupper-converts string to uppercase
 * @char*:the string to be converted to upper
 * Return:character
 */
char *string_toupper(char *)
{
	int i = 0;

	while (n[i] != '\0';)
	{
		if (n[i] >= 'a'; &&n[i] <= 'z';)
		{
			n[i] = n[i] - 32;
		}
		i++;
	}
	return (n);
