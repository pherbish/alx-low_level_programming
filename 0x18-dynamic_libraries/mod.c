#include<stdlib.h>
#include<stdio.h>
/**
 * mod-function that gives modal of two numbers
 * @a:first int
 * @b: second int
 * Return:the mode of the two integers
 */
int mod(int a, int b)
{
	return (a % b);
}
