#include<stdlib.h>
#include<stdio.h>
/**
 * sub-function that gives difference of two numbers
 * @a:first int
 * @b: second int
 * Return:the difference of the two integers
 */
int sub(int a, int b)
{
	return (a - b);
}
