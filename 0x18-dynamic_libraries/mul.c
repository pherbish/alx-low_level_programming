#include<stdlib.h>
#include<stdio.h>
/**
 * mul-function that gives product  of two numbers
 * @a:first int
 * @b: second int
 * Return:the productof the two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
